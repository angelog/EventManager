openapi: 3.0.3
info:
  title: Event Manager API
  version: 1.0.0
  description: >
    API de gerenciamento de eventos e participantes com autenticação.
    O participante autenticado cria eventos e se inscreve em eventos.
    Leitura é pública; escrita exige token (Bearer JWT).

tags:
  - name: Health

paths:
  /health:
    get:
      tags: [Health]
      summary: Health check
      responses:
        '200':
          description: API no ar
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: ok }

  /auth/register:
    post:
      tags: [Auth]
      summary: Cadastro (signup) — cria participante e retorna token
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RegisterInput' }
      responses:
        '201':
          description: Participante criado
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AuthResponse' }
        '409': { description: Email já cadastrado }
        '422': { description: Validação }

  /auth/login:
    post:
      tags: [Auth]
      summary: Login por email e senha
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/LoginInput' }
      responses:
        '200':
          description: Autenticado
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AuthResponse' }
        '401': { description: Credenciais inválidas }

  /auth/me:
    get:
      tags: [Auth]
      summary: Dados do participante autenticado
      security: [{ bearerAuth: [] }]
      responses:
        '200':
          description: Participante atual
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Participant' }
        '401': { description: Não autenticado }

    Error:
      type: object
      properties:
        error: { type: string, example: EVENT_NOT_FOUND }
        message: { type: string, example: Evento não encontrado }
        details:
          type: array
          nullable: true
          items:
            type: object
            properties:
              field: { type: string }
              message: { type: string }
